<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Listings - Your Next Stay</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        .listing { border: 1px solid #ddd; margin: 10px 0; padding: 15px; border-radius: 5px; }
        .listing img { width: 100%; max-width: 300px; height: 200px; object-fit: cover; border-radius: 5px; }
        .price { color: #e74c3c; font-weight: bold; font-size: 1.2em; }
        .location { color: #7f8c8d; }
        a { text-decoration: none; color: #3498db; }
        a:hover { text-decoration: underline; }
        .btn { background-color: #3498db; color: white; padding: 10px 20px; border: none; border-radius: 5px; cursor: pointer; }
        .btn:hover { background-color: #2980b9; }
    </style>
</head>
<body>
    <h1>All Listings</h1>
    <a href="/listings/new" class="btn">Add New Listing</a>
    
    <% if (allListings.length === 0) { %>
        <p>No listings found. <a href="/listings/new">Create the first one!</a></p>
    <% } else { %>
        <% for (let listing of allListings) { %>
            <div class="listing">
                <h3><a href="/listings/<%= listing._id %>"><%= listing.title %></a></h3>
                <img src="<%= listing.image %>" alt="<%= listing.title %>">
                <p><%= listing.description %></p>
                <p class="price">₹<%= listing.price.toLocaleString("en-IN") %></p>
                <p class="location"><%= listing.location %>, <%= listing.country %></p>
                <a href="/listings/<%= listing._id %>">View Details</a> |
                <a href="/listings/<%= listing._id %>/edit">Edit</a>
            </div>
        <% } %>
    <% } %>
</body>
</html>
